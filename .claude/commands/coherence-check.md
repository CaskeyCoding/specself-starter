---
description: Read-only vertical alignment audit across all horizons — orphans, contradictions, starved areas, capacity, staleness, drift.
---

Run a standalone SpecSelf **coherence check**. Read `steering/coherence.md` and
`steering/methodology.md`. Honor `AGENTS.md` — this is **read-only**; produce a
report, change nothing.

1. Load the stack from `your-life/`: principles, vision, goals, areas, projects,
   habits, and recent journal/reviews.
2. Walk the `serves:` links and run all six checks:
   orphans · contradictions · starved areas · over-capacity · staleness · drift.
3. Output the compact coherence report exactly as shown in `steering/coherence.md`,
   ranked — not a flat dump.
4. End with the single most important tension as one sharp question.

Do not edit any file or flip any status. Surface; the human decides.
