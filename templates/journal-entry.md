---
date: YYYY-MM-DD
tier: daily
---

# YYYY-MM-DD

Raw capture. Don't analyze, don't edit for posterity — just get the day on the
record. This is the evidence everything else is synthesized from, so honesty beats
polish.

## Responses
Today's three prompts, drawn from the rotating pool in
`templates/daily-prompts.yaml` (16 prompts, 3 per day, picked by the same
deterministic date rule the app uses, so kit and app users see the same
prompts on the same day). One prompt/answer pair per response, in the order
asked.

### Response 1: [Heading] (id [DLY-NN])
[Prompt text, verbatim from the pool.]

[Answer.]

### Response 2: [Heading] (id [DLY-NN])
[Prompt text, verbatim from the pool.]

[Answer.]

### Response 3: [Heading] (id [DLY-NN])
[Prompt text, verbatim from the pool.]

[Answer.]

## (Optional) intent for tomorrow
The one thing that would make tomorrow count.

## Captures
<!-- Timestamped raw jots appended any time via /capture. The day-close sections above are filled once, by /daily. -->

## Legacy format
Entries written before the rotating pool shipped used three fixed headers
instead of `## Responses`: `## One decision`, `## One friction`, `## One
surprise`. Those entries are valid history and are never rewritten to the new
shape.
